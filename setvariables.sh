# Defines variables that name the container and image

# The actual container and image names are prefixed with
# a period(.) or slash(/) and the user name

CONTAINER_BASE_NAME=qoolloop.mapped_container

IMAGE_BASE_NAME=qoolloop/mapped_container
